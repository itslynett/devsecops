apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-layer
  namespace: flux-system
spec:
  interval: 10m
  path: ./infrastructure/opacs/nginx  # Matches your tree layout
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
